[package]
license = "Apache-2.0"
name = "axon-markdown-core"
version = "0.1.0"
edition.workspace = true
description = "Headless markdown analysis sharing Axon's exact pulldown-cmark config."

[dependencies]
pulldown-cmark = { workspace = true }

[lints]
workspace = true
